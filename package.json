{
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "package-name": "^0.1.0",
    "webpack": "^5.93.0",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "deploy": "npm run build && gh-pages -d build",
    "build": "webpack --config webpack.config.js"
  },
  "dependencies": {
    "aphrodite": "^2.4.0"
  }
}
